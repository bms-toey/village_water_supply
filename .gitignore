# Claude Code (local IDE settings)
.claude/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Sensitive (never commit these)
.env
.env.local
*.pem
*.key
# config.js is committed — anon key is browser-safe, protected by Supabase RLS

# Build output (none currently — vanilla project)
dist/
build/

# Node (if added later)
node_modules/

# Python scripts with secrets (local only)
meter_reader.py
*.jpg
*.png
*_result.json
